# rbx-config

Subcommand: in-experience live configs via the Open Cloud Configs API. Sync/pull/check/rollback against `rbxconfig.toml`.

Part of [`rbx-forge/rbx-cli`](https://github.com/rbx-forge/rbx-cli). Not published as a standalone crate.
