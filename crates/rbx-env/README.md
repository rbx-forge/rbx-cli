# rbx-env

Subcommand: read `rbxplace.toml`: list the envs it defines, print a single resolved id for scripts, or export the map as a module for game code.

Part of [`rbx-forge/rbx-cli`](https://github.com/rbx-forge/rbx-cli). Not published as a standalone crate.
