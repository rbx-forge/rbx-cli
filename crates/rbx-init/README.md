# rbx-init

Subcommand: bootstrap Roblox resources from the CLI when nothing exists yet. Create groups, universes, places; list IDs to plug into other rbx-* subcommands.

Part of [`rbx-forge/rbx-cli`](https://github.com/rbx-forge/rbx-cli). Not published as a standalone crate.
