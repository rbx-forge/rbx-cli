# rbx-open

Subcommand: launch Roblox Studio at a specific place by env name, resolved via `rbxplace.toml`.

Part of [`rbx-forge/rbx-cli`](https://github.com/rbx-forge/rbx-cli). Not published as a standalone crate.
