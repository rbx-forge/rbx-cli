# rbx

Top-level binary that dispatches subcommands to the rbx-* domain crates. See [the workspace README](../../README.md) for usage.

Part of [`rbx-forge/rbx-cli`](https://github.com/rbx-forge/rbx-cli). Not published as a standalone crate.
