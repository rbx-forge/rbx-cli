{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "rbxmeta.toml",
  "description": "Universe and place metadata: name, description, devices, social links, server fill, media, and per-env overlays. See docs/meta.md.",
  "type": "object",
  "properties": {
    "envs": {
      "description": "Per-env overrides, layered on top of `[game]` and `[media]` when the corresponding `--env` is targeted.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/EnvOverlay"
      }
    },
    "experience": {
      "description": "Optional fallback experience IDs. Only consulted when no `--env` is passed.",
      "anyOf": [
        {
          "$ref": "#/definitions/Experience"
        },
        {
          "type": "null"
        }
      ]
    },
    "game": {
      "$ref": "#/definitions/Game"
    },
    "media": {
      "$ref": "#/definitions/MediaConfig"
    }
  },
  "definitions": {
    "AnimationType": {
      "description": "Whether players keep their own animations.",
      "type": "string",
      "enum": [
        "standard",
        "player_choice"
      ]
    },
    "AssetOverride": {
      "description": "What one avatar slot is forced to, or `player_choice` to leave it alone.\n\nWritten in TOML as either an asset id or the literal string `\"player_choice\"`:\n\n```toml [game.avatar.asset_overrides] pants = 12345678 shirt = \"player_choice\" ```\n\nAn untagged enum rather than a bare `Option<u64>` where `None` means player choice: absent and \"explicitly the player's choice\" have to stay different, because the table requires every slot (see `AssetOverrides`) and there is no third state left to spell \"not managed\" with.",
      "anyOf": [
        {
          "description": "A specific asset every player wears in this slot.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        {
          "description": "The player keeps whatever they are wearing.",
          "allOf": [
            {
              "$ref": "#/definitions/PlayerChoiceMarker"
            }
          ]
        }
      ]
    },
    "AssetOverrides": {
      "description": "The ten avatar slots Roblox lets an experience override.\n\n**Every slot is required**, for the third time in this file and for the same reason: Roblox takes `universeAvatarAssetOverrides` as one array and replaces it wholesale, so a table naming three slots is a request to reset the other seven. Since there is no endpoint that returns the array either, nothing could fill in the missing seven, not Roblox, not the lockfile. Requiring all ten makes a partial table a load error rather than a silent reset.\n\nThe `assetTypeID` each slot maps to is Roblox's global asset-type numbering, which is neither contiguous nor ordered the way this list reads: `Head` is 17 and `Torso` is 27. Hence the explicit table in `AssetOverrides::to_legacy`.",
      "type": "object",
      "required": [
        "face",
        "head",
        "left_arm",
        "left_leg",
        "pants",
        "right_arm",
        "right_leg",
        "shirt",
        "t_shirt",
        "torso"
      ],
      "properties": {
        "face": {
          "$ref": "#/definitions/AssetOverride"
        },
        "head": {
          "$ref": "#/definitions/AssetOverride"
        },
        "left_arm": {
          "$ref": "#/definitions/AssetOverride"
        },
        "left_leg": {
          "$ref": "#/definitions/AssetOverride"
        },
        "pants": {
          "$ref": "#/definitions/AssetOverride"
        },
        "right_arm": {
          "$ref": "#/definitions/AssetOverride"
        },
        "right_leg": {
          "$ref": "#/definitions/AssetOverride"
        },
        "shirt": {
          "$ref": "#/definitions/AssetOverride"
        },
        "t_shirt": {
          "$ref": "#/definitions/AssetOverride"
        },
        "torso": {
          "$ref": "#/definitions/AssetOverride"
        }
      }
    },
    "Avatar": {
      "description": "Avatar rules, as a group.",
      "type": "object",
      "properties": {
        "animation": {
          "anyOf": [
            {
              "$ref": "#/definitions/AnimationType"
            },
            {
              "type": "null"
            }
          ]
        },
        "asset_overrides": {
          "anyOf": [
            {
              "$ref": "#/definitions/AssetOverrides"
            },
            {
              "type": "null"
            }
          ]
        },
        "collision": {
          "anyOf": [
            {
              "$ref": "#/definitions/CollisionType"
            },
            {
              "type": "null"
            }
          ]
        },
        "joint_positioning": {
          "anyOf": [
            {
              "$ref": "#/definitions/JointPositioningType"
            },
            {
              "type": "null"
            }
          ]
        },
        "max_scale": {
          "anyOf": [
            {
              "$ref": "#/definitions/AvatarScales"
            },
            {
              "type": "null"
            }
          ]
        },
        "min_scale": {
          "anyOf": [
            {
              "$ref": "#/definitions/AvatarScales"
            },
            {
              "type": "null"
            }
          ]
        },
        "type": {
          "description": "`r6`, `r15`, or `player_choice`.",
          "anyOf": [
            {
              "$ref": "#/definitions/AvatarType"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "AvatarScales": {
      "description": "One end of the avatar scale range.\n\nEvery field is required rather than optional, and that is the point: Roblox takes the scales as a single object, so a table with three of the five keys would send an object Roblox reads as \"the other two are zero\". Requiring all five makes a half-written table a load error instead of a silently squashed avatar.",
      "type": "object",
      "required": [
        "body_type",
        "head",
        "height",
        "proportion",
        "width"
      ],
      "properties": {
        "body_type": {
          "type": "number",
          "format": "double"
        },
        "head": {
          "type": "number",
          "format": "double"
        },
        "height": {
          "type": "number",
          "format": "double"
        },
        "proportion": {
          "type": "number",
          "format": "double"
        },
        "width": {
          "type": "number",
          "format": "double"
        }
      }
    },
    "AvatarType": {
      "description": "Which rig players get.\n\nThe API takes an integer, and the integers are not in the order the names suggest: `PlayerChoice` sits between the two rigs. Hence the explicit mapping rather than a `#[repr]` cast.",
      "type": "string",
      "enum": [
        "r6",
        "player_choice",
        "r15"
      ]
    },
    "CollisionType": {
      "description": "The shape of an avatar's collision box.",
      "type": "string",
      "enum": [
        "inner_box",
        "outer_box"
      ]
    },
    "Devices": {
      "type": "object",
      "properties": {
        "console": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "desktop": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "mobile": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "tablet": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "vr": {
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "EnvOverlay": {
      "description": "Per-env overlay layered on top of `[game]` + `[media]` when the matching `--env` is targeted. All fields are optional; missing fields fall through to the base values.",
      "type": "object",
      "properties": {
        "allow_copying": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "avatar": {
          "$ref": "#/definitions/Avatar"
        },
        "beta_mode": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "devices": {
          "$ref": "#/definitions/Devices"
        },
        "engine_avatar_settings": {
          "type": [
            "string",
            "null"
          ]
        },
        "genre": {
          "anyOf": [
            {
              "$ref": "#/definitions/Genre"
            },
            {
              "type": "null"
            }
          ]
        },
        "media": {
          "$ref": "#/definitions/MediaOverlay"
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "paid_access": {
          "anyOf": [
            {
              "$ref": "#/definitions/PaidAccess"
            },
            {
              "type": "null"
            }
          ]
        },
        "permissions": {
          "anyOf": [
            {
              "$ref": "#/definitions/Permissions"
            },
            {
              "type": "null"
            }
          ]
        },
        "private_server": {
          "anyOf": [
            {
              "$ref": "#/definitions/PrivateServer"
            },
            {
              "type": "null"
            }
          ]
        },
        "server_fill": {
          "anyOf": [
            {
              "$ref": "#/definitions/ServerFill"
            },
            {
              "type": "null"
            }
          ]
        },
        "server_size": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        },
        "social_links": {
          "$ref": "#/definitions/SocialLinks"
        },
        "studio_access_to_apis_allowed": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "visibility": {
          "anyOf": [
            {
              "$ref": "#/definitions/Visibility"
            },
            {
              "type": "null"
            }
          ]
        },
        "voice_chat": {
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "Experience": {
      "type": "object",
      "required": [
        "place_id",
        "universe_id"
      ],
      "properties": {
        "place_id": {
          "description": "Root place ID. Used to write displayName/description/serverSize.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "universe_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      }
    },
    "Game": {
      "type": "object",
      "properties": {
        "allow_copying": {
          "description": "Allow other users to copy this place. Requires cookie (not exposed by Open Cloud).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "avatar": {
          "description": "Avatar rules: which rig, whose animations, how collisions are shaped, and the scale range players are held to.\n\nRequires cookie (universe configuration endpoint).",
          "allOf": [
            {
              "$ref": "#/definitions/Avatar"
            }
          ]
        },
        "beta_mode": {
          "description": "Beta mode toggle: limits the experience's reach by hiding it from Home Recommendations. Requires cookie (experience-releases endpoint).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "devices": {
          "$ref": "#/definitions/Devices"
        },
        "engine_avatar_settings": {
          "description": "Path to a JSON file holding the modern avatar rules, relative to this config file.\n\nThe API field is `engineAvatarSettings`, and it is a JSON *string*: a whole nested document (animation rules, clothing rules, accessory rules, collision rules, body rules) handed over as an opaque blob with no published schema for what is inside it.\n\nSo this tool does not model it. It reads the file, checks it parses as JSON, and sends it. That is a deliberate limit rather than a shortcut: the spec marks the field \"experimental which may be changed or removed in future\", and modelling a hundred and fifty keys of something Roblox reserves the right to redefine would be inventing a contract nobody offered. A file you control, versioned next to the rest of the config, keeps working whatever Roblox does to the inside of it.\n\nRoblox's own semantics line up with this file's: an absent or empty value is not written, so omitting the key leaves the settings alone. A file containing `{}` is how you clear them.\n\nRequires cookie, and write-only like the rest of `[game.avatar]`.",
          "type": [
            "string",
            "null"
          ]
        },
        "genre": {
          "description": "The legacy genre. Requires cookie (universe configuration endpoint).",
          "anyOf": [
            {
              "$ref": "#/definitions/Genre"
            },
            {
              "type": "null"
            }
          ]
        },
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "paid_access": {
          "description": "Whether the experience is sold, and for how much.\n\nOmit the table entirely to leave paid access unmanaged. Requires cookie (universe configuration endpoint).",
          "anyOf": [
            {
              "$ref": "#/definitions/PaidAccess"
            },
            {
              "type": "null"
            }
          ]
        },
        "permissions": {
          "description": "What the experience lets other experiences and the client do to it.\n\nRequires cookie (universe configuration endpoint).",
          "anyOf": [
            {
              "$ref": "#/definitions/Permissions"
            },
            {
              "type": "null"
            }
          ]
        },
        "private_server": {
          "description": "Private server settings. Omit the entire table to disable private servers.",
          "anyOf": [
            {
              "$ref": "#/definitions/PrivateServer"
            },
            {
              "type": "null"
            }
          ]
        },
        "server_fill": {
          "description": "Server fill mode. Requires cookie (not exposed by Open Cloud).",
          "anyOf": [
            {
              "$ref": "#/definitions/ServerFill"
            },
            {
              "type": "null"
            }
          ]
        },
        "server_size": {
          "description": "Max concurrent players per server.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        },
        "social_links": {
          "$ref": "#/definitions/SocialLinks"
        },
        "studio_access_to_apis_allowed": {
          "description": "Studio API access toggle. Requires cookie (universe configuration endpoint).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "visibility": {
          "description": "Public vs private visibility. Read via Open Cloud, write requires cookie.",
          "anyOf": [
            {
              "$ref": "#/definitions/Visibility"
            },
            {
              "type": "null"
            }
          ]
        },
        "voice_chat": {
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "Genre": {
      "description": "The legacy genre field.\n\nLegacy in Roblox's own sense: the discovery system has moved to experience types and tags, and this list has not changed in years. It is here because the field is still on the configuration endpoint and still round-trips, so a config that does not model it silently loses whatever it was set to on the next `pull`.",
      "type": "string",
      "enum": [
        "all",
        "tutorial",
        "scary",
        "town_and_city",
        "war",
        "funny",
        "fantasy",
        "adventure",
        "sci_fi",
        "pirate",
        "fps",
        "rpg",
        "sports",
        "ninja",
        "wild_west"
      ]
    },
    "JointPositioningType": {
      "description": "How avatar joints are positioned.",
      "type": "string",
      "enum": [
        "standard",
        "artist_intent"
      ]
    },
    "MediaConfig": {
      "type": "object",
      "properties": {
        "bleed": {
          "description": "Apply alpha bleed to PNG icons/thumbnails before uploading (default: true).",
          "default": true,
          "type": "boolean"
        },
        "dir": {
          "description": "Destination directory for `pull --accept-remote` downloads.",
          "type": [
            "string",
            "null"
          ]
        },
        "icon": {
          "description": "Path to the game icon PNG (relative to the config file).",
          "type": [
            "string",
            "null"
          ]
        },
        "language_code": {
          "description": "Language code used for localized icon/thumbnail upload (default: \"en_us\").",
          "default": "en_us",
          "type": "string"
        },
        "thumbnails": {
          "description": "Up to 10 thumbnail PNG paths, in display order.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "MediaOverlay": {
      "type": "object",
      "properties": {
        "bleed": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "dir": {
          "type": [
            "string",
            "null"
          ]
        },
        "icon": {
          "type": [
            "string",
            "null"
          ]
        },
        "language_code": {
          "type": [
            "string",
            "null"
          ]
        },
        "thumbnails": {
          "description": "`Some(vec)` overrides base thumbnails; `None` means inherit.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        }
      }
    },
    "PaidAccess": {
      "description": "Whether players pay to enter.\n\nTagged like `ServerFill` rather than modelled as a bare `Option<u64>`, because \"not for sale\" and \"not managed by this file\" are different states and a price of zero means neither. Omitting the table leaves paid access alone; `mode = \"free\"` actively turns it off.",
      "oneOf": [
        {
          "description": "Free to play.",
          "type": "object",
          "required": [
            "mode"
          ],
          "properties": {
            "mode": {
              "type": "string",
              "enum": [
                "free"
              ]
            }
          }
        },
        {
          "description": "Sold for `price` Robux.",
          "type": "object",
          "required": [
            "mode",
            "price"
          ],
          "properties": {
            "mode": {
              "type": "string",
              "enum": [
                "paid"
              ]
            },
            "price": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            }
          }
        }
      ]
    },
    "Permissions": {
      "description": "The four flags under `permissions` on the legacy universe configuration.\n\nWorth managing in a versioned file more than most settings here: each one widens what code outside the experience is allowed to do to it, they are changed rarely and by hand, and nothing inside the experience shows that one flipped. A diff is the only way anybody finds out.\n\n**All four fields are required**, and that is a consequence of the API rather than a style choice. Roblox takes `permissions` as a single object on the PATCH body, so sending one flag means sending all four, and it exposes no GET that returns them: the v1 configuration response has no `permissions` field, and the v2 endpoint answers to PATCH only. There is therefore no way to fill in the flags a partial table left out, not from Roblox and not from a first-run lockfile. Requiring all four makes a half-written table a load error instead of a write whose result nobody can predict.\n\nThe same absence of a GET means `pull` cannot adopt these: see `commands::pull`. The lockfile records what this tool last wrote, which is what `check` and `sync` compare against.",
      "type": "object",
      "required": [
        "client_teleport",
        "third_party_asset",
        "third_party_purchase",
        "third_party_teleport"
      ],
      "properties": {
        "client_teleport": {
          "description": "Whether client-initiated teleports are allowed.",
          "type": "boolean"
        },
        "third_party_asset": {
          "description": "Whether this experience may load assets it does not own.",
          "type": "boolean"
        },
        "third_party_purchase": {
          "description": "Whether this experience may prompt purchases for another creator's products.",
          "type": "boolean"
        },
        "third_party_teleport": {
          "description": "Whether another experience may teleport players into this one.",
          "type": "boolean"
        }
      }
    },
    "PlayerChoiceMarker": {
      "description": "The one string `AssetOverride` accepts in place of an id.\n\nA single-variant enum rather than a `String`, so `\"playerchoice\"` is a load error naming the valid value instead of a silently ignored slot.",
      "type": "string",
      "enum": [
        "player_choice"
      ]
    },
    "PrivateServer": {
      "type": "object",
      "required": [
        "price"
      ],
      "properties": {
        "price": {
          "description": "Price in Robux. 0 = free private servers, > 0 = paid.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      }
    },
    "ServerFill": {
      "oneOf": [
        {
          "description": "Roblox decides fill behavior automatically.",
          "type": "object",
          "required": [
            "mode"
          ],
          "properties": {
            "mode": {
              "type": "string",
              "enum": [
                "automatic"
              ]
            }
          }
        },
        {
          "description": "New players go to empty servers first.",
          "type": "object",
          "required": [
            "mode"
          ],
          "properties": {
            "mode": {
              "type": "string",
              "enum": [
                "empty"
              ]
            }
          }
        },
        {
          "description": "Reserve N slots in each server for friends/invites.",
          "type": "object",
          "required": [
            "mode",
            "reserved_slots"
          ],
          "properties": {
            "mode": {
              "type": "string",
              "enum": [
                "custom"
              ]
            },
            "reserved_slots": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            }
          }
        }
      ]
    },
    "SocialLink": {
      "type": "object",
      "required": [
        "title",
        "url"
      ],
      "properties": {
        "title": {
          "type": "string"
        },
        "url": {
          "type": "string"
        }
      }
    },
    "SocialLinks": {
      "type": "object",
      "properties": {
        "discord": {
          "anyOf": [
            {
              "$ref": "#/definitions/SocialLink"
            },
            {
              "type": "null"
            }
          ]
        },
        "facebook": {
          "anyOf": [
            {
              "$ref": "#/definitions/SocialLink"
            },
            {
              "type": "null"
            }
          ]
        },
        "guilded": {
          "anyOf": [
            {
              "$ref": "#/definitions/SocialLink"
            },
            {
              "type": "null"
            }
          ]
        },
        "roblox_group": {
          "anyOf": [
            {
              "$ref": "#/definitions/SocialLink"
            },
            {
              "type": "null"
            }
          ]
        },
        "twitch": {
          "anyOf": [
            {
              "$ref": "#/definitions/SocialLink"
            },
            {
              "type": "null"
            }
          ]
        },
        "twitter": {
          "anyOf": [
            {
              "$ref": "#/definitions/SocialLink"
            },
            {
              "type": "null"
            }
          ]
        },
        "youtube": {
          "anyOf": [
            {
              "$ref": "#/definitions/SocialLink"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "Visibility": {
      "type": "string",
      "enum": [
        "public",
        "private"
      ]
    }
  }
}
